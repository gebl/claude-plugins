{
  "name": "insecure-defaults",
  "description": "Detects insecure default configurations including hardcoded credentials, fallback secrets, weak authentication defaults, and dangerous values in production",
  "skills": "./skills",
  "interface": {
    "displayName": "Insecure Defaults",
    "shortDescription": "Detects insecure default configurations including hardcoded credentials, fallback secrets, weak authentication defaults, and ...",
    "longDescription": "Detects insecure default configurations including hardcoded credentials, fallback secrets, weak authentication defaults, and dangerous values in production",
    "category": "Developer Tools",
    "developerName": "Trail of Bits",
    "capabilities": [
      "Shell"
    ]
  }
}
